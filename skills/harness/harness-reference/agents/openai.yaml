interface:
  display_name: "Harness Reference"
  short_description: "Curate durable repository references"
  default_prompt: "Use $harness-reference to capture or maintain this external reference in the repository."
