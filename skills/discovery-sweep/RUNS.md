# Discovery Sweep — eval run log

One dated section per suite run, per [`EVALS.md`](EVALS.md).

## No runs yet

The suite has been authored but **not executed** — nothing here is a measured
result. Every case needs a throwaway repo with seeded issues and a ledger, so the
first run is a deliberate setup job, not a side effect of authoring.

First run should record: date, runner model, judge model, the fixture repo used,
the per-case matrix across ~3 rolls, and one line per failure. Expect the arming
cases (E5, E7) to be where the skill is weakest — they are the newest mechanism and
the two the two live host routines got wrong before it existed.
